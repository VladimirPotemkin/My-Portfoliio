# spa-resume

Текущий статус: завершены `Этап 1` и `Этап 2`.

Что уже сделано:

- проект переведён на `bun`;
- установлены базовые зависимости для Vue SPA;
- подключён `Tailwind CSS v4` через Vite-плагин;
- `main.ts` настроен с `Pinia`, `Router` и `MotionPlugin`;
- стартовый шаблон Vite заменён на минимальный рабочий placeholder;
- добавлен типизированный слой данных профиля и базовых сущностей приложения.

Команды:

- `bun install`
- `bun run dev`
- `bun run build`
- `bun run lint`
- `bun run typecheck`
